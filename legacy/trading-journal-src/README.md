# Trading Journal UI Source Snapshot (Exact Copy)

Esta carpeta es una copia literal de `/home/ubuntu/trading-journal/src` tomada en la VM.

Objetivo:

- Llevar a tu PC exactamente la apariencia/lógica de interfaz que ya existe en el journal web/desktop.
- Evitar reinterpretaciones visuales o migraciones parciales.

Contenido:

- `src/` copiado 1:1 desde `trading-journal/src`.

Notas:

- Este snapshot no reemplaza automáticamente `apps/web` de `finance-system`.
- Está pensado para que en tu PC puedas usar estos archivos como base exacta de migración visual.
